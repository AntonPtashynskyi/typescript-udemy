"use strict";
const obj = {
    lenovo: {
        name: "fef",
        country: "Alabama",
        createAt: new Date(),
    },
};
function printPCCatalog(catalog) {
    console.log(catalog.dell);
}
const p = {
    screen: window.screen,
};
